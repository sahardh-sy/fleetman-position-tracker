pipeline {
    agent any
   
    stages {
        stage("init") {
            steps {
                echo 'build the app'
        } }
        stage("build") {
            steps {
                echo 'build the app'
            }
        }
        stage("test") {
            
            steps {
             echo 'build the app'
        }
        }
        stage("deploy") {
            steps {
               echo 'build the app'
            }
        }
    }   
}
